"""Legacy canonical-form quark snapshots (CamelCase filenames, PM_prediction schema).

Kept alongside ``metaphysica.data.quarks`` (lowercase, new-schema) so the
``AutoGenerated/quarks/`` generator can ship the historically-expected file
shape when callers need it.
"""
